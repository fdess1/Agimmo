{% extends 'layout.html.twig' %}

{% block title %} Upload image{% endblock %}
{% block main %}
    <h1> Upload image</h1>
    {{ form_start(createForm) }}
    {{ form_widget(createForm) }}
    <input type="submit" href="{{ path('app_home_createimage') }}" value="uploader l'image" >

    {{ form_end(createForm) }}

{% endblock %}

