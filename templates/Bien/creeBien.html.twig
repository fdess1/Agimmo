{% extends 'layout.html.twig' %}

{% block title %}
    Création d'un nouveau bien immobilier
{% endblock %}
{% block main %}
    <main class="container">
        {% block bloch1 %}<h1>Création d'un nouveau bien</h1>{% endblock %}
      {#  {{ form(createForm) }}  pour glisser un bouton, on doit detailler la construction de la forme#}
        {{  form_start(createForm) }}
        {{  form_widget(createForm) }}
        <input type="submit" href="{{ path('app_bien_new_bien') }}"
               value="{% block btnvalue %}Créer le bien{% endblock %}" class="btn btn-outline-success">
        {{  form_end(createForm) }}
        {# {{ form(createForm) }}#}
    </main>
{% endblock %}


